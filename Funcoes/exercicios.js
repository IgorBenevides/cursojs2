
function cmedia (n1,n2,n3) {
     return ((n1+n2+n3) / 3)
}

media = cmedia(8,5,3);

console.log (`a media desse aluno é = ${media}`);

mediafinal = cmediafinal (media,9);

function cmediafinal (media,nf) {
   return  (media + nf )/2 
}



console.log (`a media final do aluno é = ${mediafinal}`)


function ctemperatura (gc) {
    return ((9/5)*gc)+32

}

fn = ctemperatura (32);

console.log (`a temperatura correspondente em firenght e de = ${fn}`);

function cvproduto (valorp) {
    return (((8.875*valorp)/100)+valorp)
}

valorf = cvproduto(20);

console.log (`o valor a pagar e de ${valorf}`)
